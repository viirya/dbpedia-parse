// Generated by CozyScript 0.1.1
(function() {
  var assert;

  assert = require('assert');

  describe('NetDB', function() {
    describe('#new()', function() {
      return it('should connect to neo4j db withour error', function() {
        var DB, netdb;
        DB = require('../netdb');
        netdb = new DB.NetDB();
        return netdb;
      });
    });
    describe('#findNode()', function() {
      return it('should return correct node', function(done) {
        var DB, netdb;
        DB = require('../netdb');
        netdb = new DB.NetDB;
        return netdb.findNode('BabySoul', function(node) {
          assert.strictEqual(node['title'], 'BabySoul');
          return done();
        });
      });
    });
    return describe('#findConnectedNodes()', function() {
      return it('should return correct nodes', function(done) {
        var DB, netdb;
        DB = require('../netdb');
        netdb = new DB.NetDB;
        return netdb.findConnectedNodes('BabySoul', function(nodes) {
          assert.strictEqual(nodes[0]['title'], '韓國女歌手');
          return done();
        });
      });
    });
  });

}).call(this);
